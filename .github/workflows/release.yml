name: Resource pack release
on:
  push:
    branches:
      - main
jobs:
  release:
    name: Release GitHub Actions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: gh release create latest --title "Galaar resource pack"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
